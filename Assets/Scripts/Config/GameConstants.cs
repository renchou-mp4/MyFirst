namespace yxy
{
    ///游戏场景
    public enum EGameScenes
    {
        LaunchGame = 0,
        MainGame = 1,
    }

    ///UI分组
    public enum EUIGroups
    {
        Background = 0,
        HUD,
        Dialog,
        Popup,
        Tip,
        Guide,
    }
}
